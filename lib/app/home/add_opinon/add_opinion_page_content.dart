import 'package:flutter/material.dart';

class AddOpinionPageCcontent extends StatelessWidget {
  const AddOpinionPageCcontent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ekran z Dodaj'),
    );
  }
}
